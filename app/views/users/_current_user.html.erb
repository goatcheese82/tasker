<div class="body_container">
<div class="user_card">
  <h1><%= @current_user.first_name %> <%= @current_user.last_name%></h1><%= link_to "Account Settings", edit_user_path(@current_user)%>
  <h2>Email: <%= @current_user.email %></h2>
  <h2>Address: <%= @current_user.address%></h2>
  <h2>Phone Number: <%= @current_user.phone_number%></h2>
  <img src="<%= @current_user.image %>">
</div>
</div>
  <%= link_to 'New Event', '/events/new' %>
  <br>

  <%#= render 'events/list'%>

  <br>

  <%= link_to 'Set Goal', new_user_goal_path(@current_user)%>
  <br>

  <%= render 'goals/list' %>