<div class="body_container">
<div class="user_card">
  <h1><%= @current_user.first_name %> <%= @current_user.last_name%></h1><%= link_to "Edit", edit_user_path(@current_user), :class => "edit_user" %>


  <div class="user_edit">
  </div>


  <h2>Email: <%= mail_to @current_user.email, "#{@current_user.email}"%></h2>
  <h2>Address: <%= @current_user.address%></h2>
  <h2>Phone Number: <%= @current_user.phone_number%></h2>
  <% if @current_user.image.attached? %>
  <%= image_tag @current_user.image, :class => "user_image" %>
  <%end%>

</div>
</div>
  <%= link_to 'New Event', '/events/new', :class => "create_event" %>
  <br>

  <div class="new_event">
  </div>

  <div class="events">
  </div>

  <br>

  <%= link_to 'Set Goal', new_user_goal_path(@current_user), :class => "new_goal" %>

  <br>

  <div class="goal-setter">
  </div>
  <br>

  <%= link_to 'Groups I Own', user_my_groups_path(@current_user)%>

  <%= render partial: 'goals/my_goals' %>
